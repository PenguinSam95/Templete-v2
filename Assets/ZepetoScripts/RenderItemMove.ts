import { Camera } from 'UnityEngine';
import { ZepetoScriptBehaviour } from 'ZEPETO.Script'

export default class RenderItemMove extends ZepetoScriptBehaviour {

    private drag:boolean = false;

    Start() {    

    }

    public onDraged(cam:Camera) {

    }

    public onDragEnd(cam:Camera) {
        
    }

}